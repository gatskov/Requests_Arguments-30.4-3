#include "request.h"
#define STR(text) #text
#define PRINT_TASK(SRT) \
    std::cout << STR(30.4 Practical Work.Task 3. Queries with Arguments\n-- -- -- -- -- -- -- -- -- -- -- -- -- -- -\n)

int main()
{
PRINT_TASK(STR);
TableContents();
return 0;
}